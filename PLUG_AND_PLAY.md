# PLUG_AND_PLAY — ternary-protocol

> *Integration guide.*

## Dependency

```toml
[dependencies]
ternary_protocol = "0.1.0"
```

## Feature Flags

| Feature | Default | Description |
|---------|---------|-------------|
| `std` | yes | Standard library |
| `alloc` | yes | Allocator support |

## Integration

Import `ternary_protocol` in your project to access the functionality.

```
use ternary_protocol::{};
```

## Compatibility

- **Rust edition**: 2021+
- **Targets**: All tier-1 Rust targets
